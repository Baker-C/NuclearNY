# Public Folder

This folder contains static assets that are served directly without processing.

Files placed in this folder can be referenced in your code using absolute paths starting with `/`.

For example:
- `public/logo.png` can be referenced as `/logo.png`
- `public/images/hero.jpg` can be referenced as `/images/hero.jpg`

